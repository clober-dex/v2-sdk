export type Currency = {
  address: `0x${string}`
  name: string
  symbol: string
  decimals: number
}

export type Currency6909 = {
  address: `0x${string}`
  id: bigint
  name: string
  symbol: string
  decimals: number
}
